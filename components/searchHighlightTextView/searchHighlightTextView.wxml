<!--components/searchHighlightTextView/searchHighlightTextView.wxml-->
<view class="search-name-list">
  <text wx:for="{{ searchArray }}" wx:key="*this" class="{{ item == keyName ? 'highlight' : 'normal' }}">{{
    item
  }}</text>
</view>
